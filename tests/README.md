# Tests

This directory is reserved for automated verification assets.

- `architecture/` is for structural and repository-level checks.
- `integration/` is for future component and environment integration tests.

Block 1 does not introduce business test cases, but it preserves the required test layout so later blocks can add them without restructuring the repository.

# Configuration

This directory groups configuration artifacts by concern.

- `env/` is reserved for environment templates and configuration documentation.
- `docker/` is reserved for future compose overrides or container-related assets.
- `n8n/` is reserved for workflow governance notes and runtime-specific configuration artifacts.

Secrets must not be committed. Real secret values belong in secure environment management and existing external credential stores.

# Apps

This directory is reserved for executable application services that will appear in later blocks.

- `api/` is intended for synchronous application entry points and domain orchestration.
- `worker/` is intended for asynchronous and background processing.

Block 1 intentionally does not place business logic here.

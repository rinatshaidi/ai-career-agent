# JobMonitor: текущее состояние проекта

## 2026-07-21: Block 10 production audit

- Production `jobmonitor:0.12` on `ai-prod-01` is healthy; both containers have zero restarts and no errors or warnings were found in the last 24 hours.
- SQLite `PRAGMA quick_check` returned `ok`; required tables exist, duplicate source identifiers and canonical URLs are both zero.
- All configured sources completed their latest runs independently. The latest five system runs completed successfully.
- Telegram delivery is operational: 11 notifications are recorded as sent, with no pending or failed notifications during the audit.
- Local release verification passed: 140 automated tests.
- OpenAI input, output, and total token accounting is deployed in `jobmonitor:0.13`; the first production analysis recorded 2,560 input, 1,331 output, and 3,891 total tokens.
- Daily consistent SQLite backups are configured on `ai-prod-01` at 06:15 Moscow time with seven-day retention, root-only access, and an integrity check after every copy.
- `jobmonitor:0.13` is deployed on `ai-prod-01`; both containers are healthy with zero restarts. The first cycle completed, sent one Telegram notification, and produced no provider, AI, or delivery failures.
- Block 10 and V1 are accepted as complete. Further product work belongs to a separately approved V1.1 plan.

## 2026-07-21: Block 9.1 refactor ready locally

- Replaced the two hard-coded directions with generic user-created `SearchTrack` records.
- Telegram now collects a common profile followed by one or more free-text search tracks; each track has its own locations and work formats.
- Added `/directions` and an updated persistent menu. Editing currently replaces the full profile; individual track edit/enable/delete is a later UX improvement.
- SQLite preserves existing profiles and migrates legacy fixed-direction records to `profile_search_tracks` once, without deleting legacy records.
- Local verification passed: `python -m unittest discover -s tests -p "test_*.py"` -> 138 tests.
- Not deployed to VPS yet. No production profile, token, key, `.env`, or other personal data was read or changed.

## 2026-07-21: Block 9.3 ready locally

- AI evaluates every opportunity against all enabled `SearchTrack` records and identifies the strongest direction.
- Added `priority`, `review`, and `archive` decisions. New `priority` and `review` records are eligible for Telegram without the old hard score threshold; `archive` records are not delivered.
- Telegram cards now show percentage, employment and work format, location, salary only when present, selected direction, concise match reasons, risks, required application actions, editable response draft, source, date, and link.
- SQLite migration adds nullable Block 9.3 analysis fields and keeps all existing analyses readable.
- Local verification passed: 140 automated tests.
- This local-ready note is superseded by the deployment record below.

## 2026-07-21: Block 9.3 deployed

- `jobmonitor:0.12` is deployed on `ai-prod-01`; both `jobmonitor-worker` and `jobmonitor-profile-bot` are healthy.
- Before deployment, protected backups of the `0.11` code, Compose file, and SQLite database were created on the same VPS.
- Release archive SHA-256: `7103dde5003aeeba25225c12dbfe2376faa6a75639ec7fab646562f5a0279014`.
- The first production cycle completed without provider, AI, or Telegram errors: 40 received, 2 new, 38 duplicates, 1 notification sent, 1 analysis archived/rejected.
- The production `.env` and profile contents were preserved and were not displayed or copied into project documentation.

## Утверждённый план развития проекта

**Текущая стадия:** основная рабочая цепочка JobMonitor завершена и развёрнута на VPS. Блоки 1-7 приняты. Текущая волна **Блока 8 — расширение источников** завершена: новые площадки до отдельного решения не подключаются. Следующий этап — **Блок 9.1: профиль пользователя и двухнаправленный AI-поиск**.

### Блок 8.1. Remote OK и We Work Remotely — ЗАВЕРШЁН

- источники развёрнуты на VPS и работают совместно с Habr Career;
- вакансии нормализуются, сохраняются, проходят AI-анализ и могут отправляться в Telegram.

### Блок 8.2. Независимое расписание источников — ЗАВЕРШЁН

- каждый источник имеет собственный интервал и может быть отдельно включён или выключен;
- ошибка или ожидание одного источника не останавливает обработку остальных.

### Блок 8.3. Remotive — ЗАВЕРШЁН

- Remotive включён на `ai-prod-01` 20 июля 2026 года;
- первый контролируемый production-запрос получил и сохранил 20 реальных вакансий без ошибок и дублей;
- начальный импорт ограничен 20 записями, постоянный интервал составляет 21 600 секунд;
- следующий цикл подтвердил, что источник не вызывается повторно раньше шести часов;
- первые пять Remotive-вакансий получили AI-оценки 20-30 и корректно не были отправлены в Telegram при пороге 70;
- оба production-контейнера сохранили статус `healthy`, остальные источники продолжили работу независимо.

### Блок 8.4. Greenhouse — ЗАВЕРШЁН

- образ `jobmonitor:0.5` развёрнут на `ai-prod-01`, образ `0.4` сохранён для отката;
- 97 автоматических тестов прошли в изолированном контейнере;
- доски Remote, Nebius, Fueled и SpotMe проверены live и включены последовательно;
- каждая компания работает как независимый источник с часовым интервалом;
- первый импорт ограничен 20 вакансиями на доску;
- получено и сохранено 60 вакансий: Remote — 20, Nebius — 20, Fueled — 14, SpotMe — 6;
- ошибок источников, AI и Telegram нет, оба production-контейнера имеют статус `healthy`;
- максимальная AI-оценка на момент приёмки — 65 при пороге Telegram 70, поэтому уведомлений Greenhouse пока нет.

### Блок 8.5. Межплощадочная дедупликация и контроль импорта — ЗАВЕРШЁН

- образ `jobmonitor:0.6` развёрнут на `ai-prod-01`, образ `0.5` сохранён для отката;
- URL очищаются от fragment, tracking-параметров, лишних слешей и различий регистра хоста;
- строгий отпечаток строится по компании, названию, локации и описанию;
- таблица `opportunity_sources` сохраняет все площадки и ссылки одной канонической вакансии;
- совпавшая вакансия не создаёт повторную AI-задачу;
- первый импорт нового источника ограничен 20 записями;
- при 50 ожидающих AI-записях следующий источник откладывается без расходования его интервала;
- 107 автоматических тестов прошли локально и в изолированном контейнере VPS;
- миграция сохранила 352 существующие карточки и создала для каждой запись происхождения;
- первый production-цикл `0.6` получил 20 записей, сохранил 17 и объединил 3 межплощадочных дубля;
- после цикла в базе 369 карточек и 372 записи происхождения, оба контейнера `healthy`.

### Блок 8.6. «Работа России» — ЗАВЕРШЁН

- официальный открытый API подключён без ключа и HTML-скрейпинга;
- текстовые запросы и необязательные коды регионов задаются через `.env`;
- собираются удалённые, гибридные и офисные вакансии, формат определяется консервативно;
- повторные циклы запрашивают только новые или изменённые записи через `modifiedFrom`;
- образ `jobmonitor:0.7` развёрнут на `ai-prod-01`, образ `0.6` сохранён для отката;
- 120 тестов прошли локально и в изолированном контейнере VPS;
- первый production-цикл получил и сохранил 20 реальных вакансий без ошибок;
- первые пять записей получили AI-оценку ниже порога 70 и корректно не отправлены в Telegram;
- оба production-контейнера имеют статус `healthy`.

### Блок 8.7. Arbeitnow — ОТМЕНЁН

- интеграция остановлена до изменения production-версии;
- площадка ориентирована преимущественно на рынок Германии и не соответствует текущим приоритетам пользователя;
- локальный код, временный Docker-образ и staging-файлы удалены;
- рабочая production-версия `0.7` и существующие источники не изменялись.

### Блок 8.8. Jobicy — ЗАВЕРШЁН

- до кодирования проверены назначение площадки, официальный API/RSS, география, ограничения и фактическая релевантность результатов;
- подключён официальный публичный JSON API без ключа и регистрации;
- используется один запрос `tag=automation` с лимитом 20 вакансий раз в 21 600 секунд;
- быстрые повторы отключены, а ответ принимается только при подтверждённом API-фильтре;
- сохраняются география, уровень, тип занятости и доступная информация о зарплате;
- Telegram показывает источник `Jobicy` и ведёт на исходную страницу вакансии;
- 131 автоматический тест прошёл локально и в изолированном контейнере VPS;
- SHA-256 release-архива: `4634cfcd726077afe9c8687bccf13a816706fafd98352a655140dff02f3932ec`;
- образ `jobmonitor:0.8` развёрнут на `ai-prod-01`, образ `0.7` сохранён для отката;
- первый production-цикл получил и сохранил 20 вакансий без ошибок и дублей;
- первые пять записей получили AI-оценки ниже порога 70 и корректно не отправлены в Telegram;
- оба production-контейнера работают на `0.8` со статусом `healthy`.

### Блок 8.9. Lever — ОТЛОЖЕН

- реализовать общий провайдер публичных карьерных страниц Lever;
- утвердить первоначальный список компаний;
- добавлять компании через конфигурацию без изменения кода.

### Блок 8.10. Ashby — ОТЛОЖЕН

- реализовать общий провайдер публичных карьерных страниц Ashby;
- подключить выбранные AI-, automation- и технологические компании;
- проверять каждую новую доску отдельно.

### Блок 8.11. Агрегаторы и площадки с ключами — ОТЛОЖЕН

Подключать по одному после получения и подтверждения владельцем необходимых доступов:

1. Jooble;
2. SuperJob;
3. Adzuna.

Ключи и личные данные передаются только через `.env`, не записываются в Git и документацию.

### Блок 8.12. Дополнительные официальные источники — ОТЛОЖЕН

- проверить SmartRecruiters и другие площадки с официальным API, RSS или публичными структурированными карьерными страницами;
- не применять неофициальный HTML-скрейпинг без отдельного решения;
- не подключать LinkedIn, Indeed, Wellfound, Kwork, Workzilla и аналогичные закрытые площадки, пока не подтверждён допустимый стабильный способ интеграции.

## Блок 9. Качество подбора и профиль пользователя — В РАЗРАБОТКЕ

### Блок 9.1. Двухнаправленный профиль — РАЗВЁРНУТ, ОЖИДАЕТ ПРИЁМКИ

- один пользовательский профиль в Telegram с двумя независимо настраиваемыми направлениями: `AI Automation` и `Infrastructure & Business Projects`;
- Telegram-анкета содержит не более 14 вопросов; обязателен только выбор направления, остальные ответы можно пропустить, а итоговый экран подтверждения не считается вопросом;
- профиль задаёт приоритеты, а не жёсткий отсев: уровни `priority` и `review` отправляются в Telegram, пользователь принимает окончательное решение сам;
- добавлены типизированные модели направлений `CareerDirection` и `DirectionProfile`, при этом прежний однонаправленный профиль продолжает читаться;
- SQLite-миграция добавляет таблицу `profile_directions` и поле общих условий без удаления существующих профилей;
- Telegram-бот создаёт один профиль с одним или двумя направлениями, показывает черновик и сохраняет только после явного подтверждения;
- структурированные данные двух направлений уже передаются в существующий AI-контур, но новая логика оценок и Telegram-категорий является отдельным Блоком 9.3;
- локально прошли 135 автоматических тестов; личные ответы, ключи и иные персональные данные не читались и не переносились;
- 21 июля 2026 года образ `jobmonitor:0.9` развёрнут на `ai-prod-01`; перед обновлением сохранены закрытая резервная копия кода `0.8` и rollback Compose-файл;
- release-архив проверен по SHA-256 `0a3ddb04e2124695af3cf6fdbd3f182e180c5d075871210542299d25fca6e224` и не содержит `.env`, SQLite, логов, Git-метаданных или личного профиля;
- `jobmonitor-profile-bot` и `jobmonitor-worker` работают на `0.9` со статусом `healthy`; стартовый worker-цикл завершён без ошибок и без повторного запроса источников до их очередного интервала.
- 21 июля 2026 года образ `jobmonitor:0.10` развёрнут на `ai-prod-01`; перед обновлением сохранены закрытая резервная копия кода `0.9` и rollback Compose-файл;
- release-архив `0.10` проверен по SHA-256 `b4a2f3ef962771bd5d17d8a60150254f0cf2bccd1a08538abde290ee3f95b369` и не содержит `.env`, SQLite, логов, Git-метаданных или личного профиля;
- Telegram-бот регистрирует постоянное системное меню с командами `/start`, `/profile`, `/edit_profile`; после `/start` в чате также доступны нижние кнопки «Профиль» / «Изменить профиль»;
- `jobmonitor-profile-bot` и `jobmonitor-worker` работают на `0.10` со статусом `healthy`; стартовый worker-цикл завершён без ошибок и без преждевременных повторных запросов источников.
- единые условия доступности кандидата и отдельные навыки, задачи, роли, ограничения и пороги уведомлений для каждого направления;
- AI оценивает вакансию по каждому включённому направлению, выбирает основное и объясняет решение;
- Telegram остаётся единым каналом: сообщение помечается направлением, форматом работы и географией;
- обратная связь по карточкам в этот этап не входит.

### Блок 9.2. Telegram-анкета и хранение профиля

- сценарии создания, просмотра и редактирования профиля;
- подтверждение итоговой анкеты до сохранения;
- миграция SQLite для направлений и результатов сопоставления;
- личные ответы хранятся только в production SQLite и никогда не попадают в Git, документацию или тестовые данные.

### Блок 9.3. Обновление AI-решения и уведомлений

- новый структурированный ответ AI с оценками по двум направлениям;
- отдельные пороги уведомлений и объяснение причин рекомендации;
- единая глобальная дедупликация сохраняется;
- настройки запросов к источникам не меняются без отдельного решения после приёмки профиля.

### Блок 10. Финальная проверка V1 — ЗАВЕРШЁН

Блок 10 начинается после приёмки Блока 9. Он включает полный production-аудит, проверку качества поиска, дедупликации, расходов AI, Telegram-доставки, журналов, резервного копирования, документации и тестов.

### Единый порядок выполнения каждого подэтапа Блока 8

1. До кодирования проверить назначение площадки, географию, язык, релевантность пользователю, официальный способ доступа, условия использования и лимиты.
2. Показать результат проверки пользователю и получить явное решение о подключении.
3. Реализовать провайдер и автоматические тесты локально.
4. Выполнить один ограниченный live smoke test без записи в БД, AI и Telegram.
5. Подготовить отдельную версию Docker-образа и план отката.
6. Развернуть источник на VPS выключенным по умолчанию.
7. Включить источник и провести один контролируемый production-цикл.
8. Проверить статистику: получено, сохранено, дубли, AI-очередь, уведомления и ошибки.
9. Зафиксировать итог подэтапа в этом документе и только затем переходить к следующему.

Для каждой внешней операции допускается не более двух попыток. После второй неудачи работа останавливается, причина объясняется пользователю, а дальнейшее решение принимается отдельно.

## Блок 8.6: интеграция «Работа России»

**Production-статус:** завершено и принято на `ai-prod-01` 20 июля 2026 года. Оба контейнера работают на образе `jobmonitor:0.7`.

- используется официальный открытый GET API `https://opendata.trudvsem.ru/api/v1/vacancies`;
- API не требует ключа, запросы выполняются по HTTPS и возвращают JSON;
- поисковые фразы задаются через `TRUDVSEM_SEARCH_QUERIES`, регионы — через необязательный `TRUDVSEM_REGION_CODES`;
- глобальный поиск по России работает при пустом списке регионов;
- `TRUDVSEM_POLL_INTERVAL_SECONDS=3600`, `TRUDVSEM_PER_QUERY_LIMIT=10`, общий лимит источника — 20;
- первый запуск ограничен периодом 14 дней, следующие используют `last_success_at` с пятиминутным перекрытием и параметр `modifiedFrom`;
- одинаковая вакансия, найденная по нескольким фразам, объединяется по внешнему ID ещё до общей дедупликации;
- контактное лицо, телефон и email работодателя из ответа API не сохраняются;
- удалёнка и гибрид определяются только по явным фразам, слово с корнем «удален» само по себе не считается удалённой работой;
- при наличии адреса и отсутствии признаков удалёнки вакансия помечается как офисная;
- штатный ответ API `status=200` с пустым `results={}` обрабатывается как отсутствие новых записей;
- release-архив не содержит `.env`, базы, логов, личного профиля, Git-метаданных или байткода;
- SHA-256 принятого архива: `028263f70fb040e751776bd874b2fbbadb20cfe3ceb42d53b440c809d0735419`;
- 120 тестов прошли локально и в контейнере `jobmonitor:0.7` без сети;
- два изолированных live smoke test получили по три реальные записи без обращения к production-базе, OpenAI и Telegram;
- первый production-цикл: получено 20, сохранено 20, дублей 0, отложено 0, ошибка отсутствует;
- первые пять записей отклонены AI как нерелевантные; уведомлений и ошибок Telegram нет;
- `source_states` содержит статус `completed`, а `opportunity_sources` — 20 наблюдений источника;
- предыдущий образ `jobmonitor:0.6` и `docker-compose.yml.rollback-0.6` сохранены для отката;
- временные staging-файлы на VPS удалены после приёмки;
- GitHub не изменялся.

## Блок 8.5: межплощадочная дедупликация и контроль импорта

**Production-статус:** завершено и принято на `ai-prod-01` 20 июля 2026 года. Оба контейнера работают на образе `jobmonitor:0.6`.

- перед обновлением сохранены закрытые резервные копии кода, `.env` и согласованная копия SQLite;
- миграция сначала выполнена на копии production-базы и только после проверки применена к рабочей базе;
- `canonical_url` используется для совпадений одной ссылки с разными tracking-параметрами;
- `content_fingerprint` является SHA-256 отпечатком нормализованных компании, названия, локации и описания;
- `opportunity_sources` сохраняет исходный URL, площадку, внешний ID и время первого и последнего обнаружения;
- `MAX_PENDING_AI_QUEUE=50` ограничивает очередь новых записей;
- `SOURCE_INITIAL_IMPORT_LIMIT=20` ограничивает первый импорт каждого нового источника;
- отложенные записи и источники учитываются в статистике pipeline и `source_runs`;
- полный набор из 107 тестов прошёл локально и внутри образа `0.6` без доступа к сети;
- первый production-цикл завершён без ошибок: получено 20, сохранено 17, объединено 3, AI-ошибок 0, Telegram-ошибок 0;
- production-база содержит 369 канонических карточек, 372 записи происхождения и 3 подтверждённые карточки с несколькими источниками;
- `jobmonitor-worker` и `jobmonitor-profile-bot` имеют статус `healthy`.

## Блок 8.4: интеграция Greenhouse

**Production-статус:** завершено и принято на `ai-prod-01` 20 июля 2026 года. Оба контейнера работают на образе `jobmonitor:0.5`.

- добавлен публичный Greenhouse Job Board API без API-ключа;
- каждая компания создаётся как независимый источник `greenhouse_<board_token>`;
- список компаний настраивается через `.env` без изменения кода;
- Greenhouse по умолчанию выключен до явного утверждения списка компаний;
- стартовый production-кандидат: Remote, Nebius, Fueled и SpotMe как доски с глобальными или европейскими удалёнными ролями;
- полный текст, отделы, офисы, язык, локация и ссылка нормализуются в `Opportunity`;
- название компании берётся из конфигурации, технический board token в Telegram не показывается;
- безопасный live smoke test доски `lindawernerassociates` получил и нормализовал одну реальную вакансию;
- версия Greenhouse развёрнута отдельным образом `jobmonitor:0.5`; предыдущий production-образ `0.4` сохранён для отката;
- production SQLite и секреты сохранены без потери данных, GitHub не изменялся.

Production-приёмка:

- release-архив проверен по SHA-256 и не содержит `.env`, базы, логов, личного профиля или байткода;
- отдельный образ `jobmonitor:0.5` собран без остановки production `0.4`;
- 97 тестов прошли в изолированном контейнере без доступа к сети;
- live smoke test каждой доски прошёл без записи в БД, OpenAI и Telegram;
- перед обновлением сохранена резервная копия кода `0.4`, сам образ `jobmonitor:0.4` оставлен на VPS;
- версия `0.5` сначала развёрнута с выключенным Greenhouse и проверена на прежних источниках;
- Remote, Nebius, Fueled и SpotMe включены по одной, между включениями каждый pipeline-цикл завершался штатно;
- результаты первого импорта: Remote — 20, Nebius — 20, Fueled — 14, SpotMe — 6;
- все четыре `source_states` имеют статус `completed` и пустое поле ошибки;
- контрольный цикл подтвердил, что повторный запрос Greenhouse не выполняется раньше 3 600 секунд;
- AI обрабатывает очередь пакетами по пять, Telegram отправляет только результаты с оценкой не ниже 70;
- на момент приёмки максимальная оценка Greenhouse составила 65, поэтому уведомлений не было;
- `jobmonitor-worker` и `jobmonitor-profile-bot` работают на `jobmonitor:0.5` и имеют статус `healthy`.

## Блок 8.3: интеграция Remotive

**Production-статус:** завершено и принято на `ai-prod-01` 20 июля 2026 года. Remotive активен с `REMOTIVE_VACANCY_LIMIT=20` и `REMOTIVE_POLL_INTERVAL_SECONDS=21600`.

- добавлен провайдер официального публичного JSON API Remotive;
- вакансии нормализуются в общую модель `Opportunity`;
- исходная ссылка Remotive и название источника сохраняются для атрибуции;
- Remotive имеет собственный интервал 21 600 секунд, то есть не более четырёх запусков в сутки;
- конфигурация запрещает установить интервал Remotive меньше шести часов;
- быстрые повторы ошибок для Remotive отключены: один разрешённый запуск выполняет один HTTP-запрос;
- состояние последней попытки хранится в SQLite, поэтому `--once` и перезапуск worker не обходят расписание;
- безопасный live smoke test с `limit=1` успешно получил и нормализовал одну реальную запись;
- полный локальный набор: 84 теста проходят;
- production SQLite и секреты сохранены без изменений; GitHub не изменялся;
- перед обновлением создана резервная копия кода `0.3`, рабочий образ `jobmonitor:0.3` оставлен для отката.

Production-приёмка:

- создана закрытая резервная копия production `.env` перед активацией;
- Compose-конфигурация проверена до пересоздания worker;
- пересоздан только `jobmonitor-worker`, профильный Telegram-бот не изменялся;
- первый запуск Remotive завершён со статусом `completed`: получено 20, сохранено 20, дублей 0, ошибок 0;
- первые пять записей обработаны AI без ошибок и отклонены с оценками ниже порога уведомления;
- контрольный цикл пропустил Remotive с интервалом 21 600 секунд, исключив повторный API-запрос;
- Habr Career, Remote OK и We Work Remotely продолжили работу;
- `jobmonitor-worker` и `jobmonitor-profile-bot` имеют статус `healthy`.

## Блок 8: независимое расписание источников

**Production-статус:** развёрнут и технически принят на `ai-prod-01`
on the dedicated production VPS on 19 July 2026.

- основной worker остаётся единым и продолжает выполнять полный pipeline;
- Habr Career, Remote OK и We Work Remotely получили независимые интервалы опроса;
- каждый источник можно отдельно включить или отключить через `.env`;
- SQLite хранит время последней попытки и последнего успешного запуска источника;
- история запусков содержит статус, ошибку и счётчики полученных, сохранённых и повторных записей;
- перезапуск worker не сбрасывает расписание источников;
- аварийно прерванные запуски источников восстанавливаются как неуспешные;
- пропуск источника до наступления его интервала не мешает AI-анализу и Telegram-доставке уже собранных записей;
- все 76 автоматических тестов проходят;
- оба production-контейнера работают на образе `jobmonitor:0.3` и имеют статус `healthy`;
- первый production-цикл получил по 20 записей из каждого из трёх источников без ошибок;
- контрольный перезапуск подтвердил сохранение расписания: все три источника были
  корректно пропущены до наступления своих интервалов;
- перед обновлением создана проверенная резервная копия версии `0.2`.

GitHub в рамках Блока 8 не изменялся.

## Интеграции Remote OK и We Work Remotely

**Production-статус:** развёрнуто и принято на выделенном production VPS 18 июля 2026 года.

- добавлен официальный JSON API Remote OK;
- добавлен официальный публичный RSS We Work Remotely;
- оба источника нормализуются в общую модель `Opportunity`;
- ссылки на исходные страницы сохраняются для атрибуции и перехода к вакансии;
- Habr Career остаётся активным источником;
- сбой одной площадки не останавливает остальные;
- все 70 автоматических тестов проходят;
- обе живые публичные ленты успешно прошли безопасную проверку без записи в БД, AI-запросов и Telegram-уведомлений.
- оба production-контейнера работают на образе `jobmonitor:0.2` и имеют статус `healthy`;
- первый production-цикл получил 60 записей из трёх источников, сохранил 40 новых и завершился без ошибок;
- после первого цикла в базе: Habr Career — 75, Remote OK — 20, We Work Remotely — 20;
- 35 новых записей последовательно обрабатываются AI пакетами по 5;
- старый образ `jobmonitor:0.1` и резервная копия кода сохранены для отката.

## Обновление после реализации Блока 6

**Статус Блока 6:** завершён и принят на реальном полном цикле

Реализовано:

- единый автоматический pipeline `сбор → сохранение → AI-анализ → Telegram`;
- основной процесс `python worker.py` с немедленным первым запуском и настраиваемым интервалом;
- диагностический однократный режим `python worker.py --once`;
- ограниченные повторы временных ошибок RSS, OpenAI и Telegram;
- изоляция источников: ошибка одного провайдера не останавливает остальные этапы и worker;
- корректная остановка по `SIGINT` и `SIGTERM`;
- rotating file log и консольный журнал без секретов;
- SQLite-таблица `system_runs` с результатом каждого цикла;
- восстановление записи о цикле, прерванном аварийной остановкой;
- 52 автоматических теста проходят.

Live-приёмка `python worker.py --once`:

- Habr Career вернул 20 записей: 4 новых и 16 дублей;
- AI успешно обработал пакет из 5 записей: 1 подходящая и 4 отклонённых;
- одна подходящая возможность доставлена в Telegram;
- ошибок источника, AI и Telegram нет;
- цикл записан в `system_runs` со статусом `completed`;
- контрольный запуск доставки отправил 0 повторных сообщений.

Следующий этап: **Блок 7 — Docker и VPS**.

## Обновление после Блока 5

**Статус Блока 4:** завершён и принят на live-данных  
**Статус Блока 5:** завершён и принят через реальную отправку в Telegram

Фактическое состояние:

- Telegram-чат привязан без хранения идентификатора в документации;
- пользовательский профиль заполнен и подтверждён через Telegram;
- OpenAI Responses API выполнил реальный пакетный анализ со `store=false`;
- структурированные результаты сохраняются в SQLite;
- Telegram получает только подходящие результаты с оценкой не ниже `MIN_AI_SCORE`;
- создана таблица `telegram_notifications` с уникальностью по opportunity;
- успешная доставка переводит opportunity в `notified`;
- ошибка доставки сохраняется и допускает повтор;
- повторный запуск не отправляет уже доставленное сообщение;
- live-проверка: одна подходящая возможность отправлена, ошибок доставки нет;
- автоматическая проверка: 46 тестов проходят.

Следующий этап: **Блок 6 — единый автоматический цикл, расписание, журналирование и эксплуатационная надёжность**.

**Дата аудита:** 17 июля 2026 года  
**Формат аудита:** только локальные файлы  
**Изменения во время аудита:** не выполнялись

## Обновление после Блока 1

**Статус Блока 1:** завершён  
**Активный источник:** официальный RSS Habr Career

После первоначального аудита выполнены следующие изменения:

- HeadHunter удалён из активного кода и спецификации;
- создан production-провайдер `providers/habr.py`;
- `main.py` переключён на Habr Career;
- добавлены настройки Habr Career RSS с безопасными значениями по умолчанию;
- добавлены тесты RSS-парсера, HTTP-ошибок и конфигурации;
- исправлен вывод русскоязычных вакансий и символа рубля в Windows-консоли;
- выполнен реальный smoke test официального RSS: приложение успешно получило 20 вакансий;
- все 9 автоматических тестов проходят.

## Обновление после Блока 2

**Статус Блока 2:** завершён  
**Результат:** Habr Career преобразуется в единую типизированную модель `Opportunity`

Выполнено:

- создана неизменяемая доменная модель `models/opportunity.py`;
- добавлена валидация обязательных полей, URL, зарплаты, валюты и дат;
- введён общий контракт источников `OpportunityProvider`;
- Habr Career реализован как `HabrCareerProvider` и больше не возвращает словари;
- нормализуются идентификатор, заголовок, описание, компания, локация,
  формат работы, зарплата, валюта, дата публикации, ссылка и время сбора;
- `main.py` работает только с общим контрактом и объектами `Opportunity`;
- автоматический набор расширен до 14 тестов;
- синтаксическая проверка всех Python-модулей проходит успешно;
- live smoke test официального RSS получил 20 реальных вакансий и подтвердил
  нормализацию названия, компании, города, удалённого формата и зарплаты.

Следующий этап: **Блок 3 — локальное хранение, дедупликация и статусы обработки**.

## Обновление после Блока 3

**Статус Блока 3:** завершён  
**Хранилище MVP:** SQLite (`data/jobmonitor.db`)

Выполнено:

- создан SQLite-репозиторий `storage/repository.py`;
- схема базы создаётся автоматически при первом запуске;
- все поля `Opportunity` сохраняются без зависимости от Habr Career;
- уникальная пара `source + external_id` защищает от повторной записи;
- добавлены статусы `new`, `analyzing`, `analyzed`, `notified`, `rejected`, `failed`;
- доступны выборка по статусу, изменение статуса и учёт ошибки;
- `main.py` сохраняет только новые возможности и показывает число дублей;
- путь базы настраивается через `DATABASE_PATH`;
- файлы рабочей SQLite-базы исключены из Git.

Приёмка выполнена на официальном live RSS Habr Career:

- первый запуск: получено 20, сохранено 20, дублей 0;
- повторный запуск: получено 20, сохранено 0, дублей 20;
- итог в базе: 20 уникальных возможностей со статусом `new`;
- все 21 автоматический тест проходит.

Следующий этап после приёмки: **Блок 4 — AI-анализ новых возможностей**.

## Обновление по Блоку 4

**Статус Блока 4:** код реализован, live-приёмка ожидает рабочий Telegram-токен  
**AI API:** OpenAI Responses API + Structured Outputs

Реализовано:

- модель результата `AIAnalysis` с локальной валидацией;
- модель `CandidateProfile` и хранение подтверждённого профиля в SQLite;
- Telegram-анкета `/start`, `/profile`, `/edit_profile`, `/skills`;
- сохранение и восстановление незавершённой анкеты;
- ограничение доступа заданным `TELEGRAM_CHAT_ID`;
- защита токена от вывода в HTTP-ошибках и traceback;
- обезличенный JSON-пример без персональных данных;
- OpenAI-клиент со строгой JSON Schema и `store=false`;
- защита промпта от инструкций внутри текста вакансии;
- обработка HTTP-ошибок, некорректного ответа и отказа модели;
- таблица `ai_analyses` и атомарное сохранение результата в SQLite;
- пакетная очередь `new → analyzing → analyzed/rejected/failed`;
- отдельная команда `python analyze.py`;
- 40 автоматических тестов проходят.

Live-проверка показала, что `TELEGRAM_TOKEN` в локальном `.env` пока не является
рабочим Bot API token. До его замены бот не запускается. Профиль и тексты
вакансий в OpenAI не отправлялись, API-расходы не создавались.

## Исходный аудит до Блока 1

Проект `JobMonitor` находится на стадии раннего Python-прототипа. Это отдельный проект, а не большая n8n-система `AI Career Agent`, описанная в предыдущих обсуждениях.

Фактически реализован только диагностический запрос к API HeadHunter и вывод пяти результатов в консоль. AI-анализ, Telegram, история, планировщик, Docker, тесты и Habr Career пока отсутствуют.

Текущая ориентировочная готовность: **10–15% от описанного MVP**.

## Что реально создано

В проекте присутствуют:

- `SPECIFICATION.md` — техническое задание версии `0.1`;
- `main.py` — точка запуска приложения;
- `config.py` — загрузка и проверка настроек;
- `providers/hh.py` — единственный реализованный источник вакансий;
- `.env.example` — шаблон настроек без секретов;
- `requirements.txt` — зависимости `httpx` и `python-dotenv`;
- `.gitignore` — исключение `.env`, кэша Python и скомпилированных файлов.

Локальный `.env` существует, обязательные параметры заполнены. Их значения во время аудита не раскрывались и в этот отчёт не включены.

Все существующие Python-файлы синтаксически корректны.

## Что реализовано

Текущий сценарий работы:

1. Загружается локальный `.env`.
2. Проверяются обязательные настройки OpenAI, Telegram и интервала запуска.
3. Выполняется HTTP-запрос к API HeadHunter.
4. Запрашиваются пять вакансий по тестовому запросу `Python`.
5. HTTP-диагностика и найденные вакансии выводятся в консоль.

Файлы, непосредственно связанные с HeadHunter:

- `main.py` — импортирует и запускает HH-провайдер, обрабатывает его ошибки;
- `providers/hh.py` — содержит URL API, тестовый поисковый запрос и разбор ответа;
- `SPECIFICATION.md` — описывает HeadHunter как единственный источник MVP.

## Что пока отсутствует

Не реализованы:

- Habr Career;
- AI-анализ вакансий;
- отправка сообщений в Telegram;
- единая нормализованная модель вакансии;
- дедупликация;
- хранение истории;
- планировщик периодических запусков;
- PostgreSQL или другая база данных;
- Dockerfile и Docker Compose;
- производственное логирование;
- автоматические тесты;
- работа приложения 24/7;
- Google Sheets;
- n8n workflow.

Каталоги `services`, `storage`, `utils`, `prompts`, `logs` и `tests`, заявленные в спецификации, физически не созданы.

## Где остановилась разработка

Разработка остановилась после создания проверочного подключения к HeadHunter API.

Полного рабочего конвейера пока нет:

```text
Источник
  -> нормализация
  -> AI-анализ
  -> фильтрация
  -> Telegram
  -> история
```

Проект нельзя считать production-ready. На текущий момент готовы только конфигурационная основа и диагностический HH-клиент.

## Расхождения со спецификацией

`SPECIFICATION.md` целиком построен вокруг HeadHunter. HH указан:

- в цели проекта;
- как единственный источник MVP;
- в основном сценарии;
- в архитектуре;
- в структуре каталогов;
- в Definition of Done.

После решения больше не развивать HeadHunter спецификацию необходимо существенно обновить.

Есть отдельное архитектурное противоречие: PostgreSQL исключён из MVP в локальной спецификации, хотя в обсуждавшемся ранее проекте AI Career Agent PostgreSQL являлся центральным хранилищем. До дальнейшей реализации необходимо определить, какая архитектура относится именно к `JobMonitor`.

## Файлы для перехода на Habr Career

Обязательно потребуется:

- изменить `SPECIFICATION.md`, заменив требования HeadHunter требованиями Habr Career;
- изменить `main.py`, заменив импорт, вызов и сообщения HH;
- вывести `providers/hh.py` из активного использования или удалить после подтверждения;
- создать `providers/habr.py` с реализацией нового источника.

В зависимости от выбранного официального способа интеграции также может потребоваться:

- изменить `requirements.txt`, если понадобятся дополнительные библиотеки;
- изменить `.env.example` и `config.py`, если Habr Career потребует отдельные параметры;
- добавить общий контракт провайдера;
- добавить тесты нормализации и обработки ошибок.

Docker менять сейчас нечего: Docker-файлы в локальном проекте отсутствуют.

## Риски и технический долг

- Официальный стабильный способ получения данных из Habr Career пока не подтверждён. Его необходимо исследовать до реализации.
- HH-провайдер выводит полные HTTP-заголовки и тело ответа. Для production это избыточно и потенциально небезопасно.
- В HH-провайдере присутствуют захардкоженные контактные данные. При переходе на новый источник их необходимо удалить из кода, не перенося в новый провайдер.
- `main.py` требует настройки OpenAI и Telegram, хотя текущий код ими не пользуется.
- Каталог `.git` пустой, поэтому локальная папка фактически не является Git-репозиторием.
- Автоматические тесты отсутствуют.
- Реальный сетевой запрос во время аудита не выполнялся, поэтому работоспособность API в текущей среде не подтверждена.

## Рекомендуемый следующий этап

1. Проверить допустимый официальный механизм интеграции Habr Career.
2. Зафиксировать общий контракт провайдера вакансий.
3. Обновить спецификацию под Habr Career.
4. Реализовать `providers/habr.py`.
5. Переключить `main.py` на новый провайдер.
6. Добавить единую модель нормализации и тесты.
7. После стабилизации источника последовательно реализовать AI, Telegram, хранение и Docker.

## Ограничения аудита

- Проверялись только локальные файлы в корне проекта `JobMonitor`.
- GitHub, VPS, n8n и PostgreSQL не проверялись и не изменялись.
- Секреты, токены, ключи и значения `.env` в отчёт не включены.
- Никакие действия с GitHub не выполнялись.
